[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "yuki-code"
version = "1.0.0"
description = "Yuki Code — 本地 AI 代码助手"
requires-python = ">=3.10"
dependencies = ["requests>=2.31.0", "rich>=13.7.0"]

[project.scripts]
yuki = "cli.__main__:main"
